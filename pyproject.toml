[project]
name = "final-whistle"
version = "0.1.0"
description = "Football analytics learning project"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0",
]
[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]